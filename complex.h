//My implemetation of standard c++ library. Needed because android-ndk hasn't got one.
using namespace std;
template <typename T>
struct complex <T> {
		T r;
		T i;
		complex()s
		complex(T r, T i)
}
//operators overloading for complex
T & operator+(const & T, const & T);
